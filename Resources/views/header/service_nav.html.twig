{# Note:
  This template is rendered twice by default
  (included in header/page_header.html.twig and haeder/category_nav.html.twig)
 #}
<div class="service-nav">

    {% block nav_pages %}
{#    <nav class="service-nav__pages">#}
{#        <a href="#" class="service-nav__page">Kontakt</a>#}
{#        <a href="#" class="service-nav__page">Impressum</a>#}
{#        <a href="#" class="service-nav__page">AGB</a>#}
{#    </nav>#}
    {% endblock %}

    {% set locales = paloma.locales %}
    {% if locales|length > 1 %}
        <nav class="service-nav__locales locales">
            {# TODO show drop down if there are lots of locales #}
            {% for locale in locales %}
                <a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({'_locale': locale})) }}"
                   class="locales__item{% if locale == app.request.locale %} locales__item--active{% endif %}">
                    {{ ('locale.' ~ locale) | trans }}
                </a>
            {% endfor %}
        </nav>
    {% endif %}
</div>